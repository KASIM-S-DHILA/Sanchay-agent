/// <reference types="@cloudflare/workers-types" />

// ponytail: `Ai` ships as an abstract class in @cloudflare/workers-types,
// and worker-configuration.d.ts doesn't exist yet (no `wrangler types` run).
// This file is the anchor for future generated-types references.
